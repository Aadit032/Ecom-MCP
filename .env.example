# Postgres (default matches docker-compose.yml)
DATABASE_URL="postgresql://ecom:ecom@localhost:5432/ecom_mcp?schema=public"

# Required for write tools: reset_seed_data, issue_refund, resolve_escalation
WRITE_TOKEN="replace-with-a-long-random-secret"

# Server
PORT=3000
HOST=0.0.0.0
